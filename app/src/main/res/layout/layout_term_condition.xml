<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@color/white"
    android:backgroundTint="@color/white"
    android:layout_height="match_parent">

    <Toolbar
        android:id="@+id/tbrTermCondition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:navigationIcon="@drawable/ic_back">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/circular_std_black"
            android:text="@string/terms_and_conditions_label_adscreen"
            android:textColor="@color/main_text_color"
            android:textSize="20sp" />
    </Toolbar>

    <View
        android:id="@+id/viewBelowTbr"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/tbrTermCondition"
        android:alpha="0.3"
        android:background="@color/main_text_color" />


    <ProgressBar
        android:id="@+id/pgbTermCondition"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingBottom="16dp" />

    <WebView
        android:id="@+id/webViewTermCondition"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/viewBelowTbr"
        android:textDirection="locale"
        android:visibility="gone" />

</RelativeLayout>