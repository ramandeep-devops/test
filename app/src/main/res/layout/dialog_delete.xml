<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/back_filled_white_rounded_corner"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="24dp"
    android:paddingBottom="24dp"
    android:layout_marginStart="24dp"
    android:layout_marginEnd="24dp"
    android:layout_gravity="center"
    android:gravity="center_horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/ivDeleteDialogIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_delete_big"
        android:contentDescription="@string/cdesc_delete_icon" />

    <TextView
        android:id="@+id/tvDeleteDialogHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="48dp"
        android:layout_marginEnd="48dp"
        android:textColor="@color/main_text_color"
        android:fontFamily="@font/circular_std_book"
        android:textSize="16sp"
        android:layout_marginTop="16dp"
        android:gravity="center"
        tools:text="Are you sure you want to remove this study from your portfolio ?"
        />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            >
            <Button
                android:id="@+id/btnDeleteDialogCancel"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:text="@string/cancel_label_dialog_delete"
                android:textSize="12sp"
                android:gravity="center"
                android:textColor="@color/white"
                android:fontFamily="@font/circular_std_bold"
                android:background="@drawable/back_filled_grey_full_rounded"
                tools:ignore="ButtonStyle" />

            <Button
                android:id="@+id/btnDeleteDialogRemove"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:text="@string/remove_label_delete_dialog"
                style="@style/FullRoundedAccentColorButton"
                android:layout_marginStart="8dp"
                android:textSize="12sp"
                />

        </LinearLayout>
</LinearLayout>